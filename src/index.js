import React from "react";
import ReactDOM from "react-dom";

const Index = () => {
    return <>
        <h1 className="header">Simple React app using webpack</h1>
    </>;
};

ReactDOM.render(<Index />, document.getElementById("root"));
